import React from 'react';

// Logo3D previously rendered a WebGL logo; project no longer uses three.js by default.
// This lightweight stub prevents build errors if accidentally imported.
export default function Logo3D() {
  return null;
}

